# Notebook 03 — CNN Training Notes

The optional model is a small U-Net.

Input channels:

```text
R, G, B, DSM
```

Output classes:

```text
background, building, tree, road
```

Suggested metrics:

- pixel accuracy
- class IoU
- precision
- recall
- F1-score
